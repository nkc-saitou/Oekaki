using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Panel : MonoBehaviour {

    public Material mat;

    void Awake()
    {
        //var r = GetComponent<Renderer>();
        //r.material = Instantiate(mat);
    }

    // Use this for initialization
    void Start () {

    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
